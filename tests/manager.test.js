const Employee = require('../lib/Employee')
const Manager = require('../lib/Manager')

describe('manager', () => {
it('should return phone number', () => {
if (Employee === Manager) {
return officeNumber
}
}) 
 })


