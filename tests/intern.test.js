const Employee = require('../lib/Employee')
const Intern = require('../lib/Intern')

describe('intern', () => {
it('should return school', () => {
  if (Employee === Intern) {
    return school
  }
}) 
 })

